<!-- 

Member Variable
    String[] fruits = {"peach", "mango", "banana"};
    System.out.printf("Length: %s", arr.length);
    ArrayList<Integer> nums = new ArrayList<Integer>();
    nums.add(42);
So if add() is an ArrayList method, what is length? Length is an example of what's called a member variable in Java.  

-->
